Логическая и физическая структура баз данных

Для эффективной организации данных и их обработки важно понимать две основные структуры БД:

Логическая структура — определяет, как данные организованы и связаны с точки зрения пользователя и разработчика.
Физическая структура — описывает, как данные хранятся в памяти и на диске.
Разберём каждую структуру подробно.

1. Логическая структура базы данных
Логическая структура описывает представление данных на концептуальном уровне. Это то, как данные воспринимаются пользователем или программистом.

Основные компоненты логической структуры:
Схема базы данных – определяет таблицы, их связи, атрибуты и ограничения.
Таблицы (отношения) – основные объекты реляционной БД, хранящие данные в виде строк и столбцов.
Атрибуты (поля, столбцы) – отдельные характеристики объекта, например, имя, возраст.
Связи между таблицами – определяют, как данные одной таблицы связаны с другой (1:1, 1:M, M:M).
Ограничения (constraints) – правила, регулирующие целостность данных (например, PRIMARY KEY, FOREIGN KEY).
Пример логической структуры
Допустим, у нас есть база данных для интернет-магазина. Она содержит две таблицы:

sql
Копировать
Редактировать
CREATE TABLE Users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100) UNIQUE
);

CREATE TABLE Orders (
    id SERIAL PRIMARY KEY,
    user_id INT,
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    total_amount DECIMAL(10, 2),
    FOREIGN KEY (user_id) REFERENCES Users(id)
);
Здесь:

Users и Orders — таблицы.
id, name, email — атрибуты.
FOREIGN KEY связывает заказы с пользователями.
🔹 Логическая структура описывает связи между объектами, но не указывает, как именно данные хранятся на диске.

2. Физическая структура базы данных
Физическая структура определяет способы хранения данных в памяти и на диске.

Основные компоненты физической структуры:
Файлы данных – физические файлы на диске, в которых хранятся таблицы.
Индексы – структуры, ускоряющие поиск данных.
Журнал транзакций – лог изменений, обеспечивающий надёжность БД.
Буферный кэш – область в оперативной памяти для быстрого доступа к данным.
Страницы и блоки данных – минимальные единицы хранения информации.
Как физически хранятся данные?
Данные делятся на блоки (pages), которые загружаются в оперативную память при запросах.
Индексы организованы в виде B-деревьев или хэш-таблиц для быстрого поиска.
Файлы данных имеют различные форматы (.mdf, .ndf в SQL Server, .ibd в MySQL).
Пример физического хранения
В MySQL файлы .frm, .ibd содержат схемы и данные.
В PostgreSQL файлы /var/lib/postgresql/data содержат таблицы и индексы.